# Logintel Agent

Logintel Agent is a open source Golang-based agent which captures different logs from OS. The agent is currently supports Linux kernel only. It detects different logs like command execution, TCP/UDP connection, file events etc.